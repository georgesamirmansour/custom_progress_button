name: custom_progress_button
description: A new Flutter project.
homepage: https://github.com/georgesamirmansour/progressButton

version: 1.0.0

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
